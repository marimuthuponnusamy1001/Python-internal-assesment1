# -*- coding: utf-8 -*-
"""Untitled4.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1NLmrwasOeWmZl_kWqZtG6xVXh_6qHibj
"""

#1
length = float(input("enter the length of rectangle" ))
width = float(input("enter the width of the rectangle" ))
area  = 2*(length*width)
print("area of the rectangle is  ",  area)

#2
weight = float(input("enter the weight in kg"))
heigth = float(input("enter the heigth in meter"))
bmi = (weight /(heigth**2))
print(bmi)

#3
students = {}
def add_student():
  name = input("Enter student name: ")
  scores = {}
def print_students():
  if not students:
    print("No students added yet.")
    return
  for name, scores in students.items():
      print(f"{name}: {score}")
while True:
  print("\nMenu:")
  print("1. Add Student")
  print("2. Print Students")
  print("3. Exit")
  choice = input("Enter your choice: ")
  if choice == '1':
    add_student()
  elif choice == '2':
    print_students()
  elif choice == '3':
    print("Exits.")
    break
  else:
    print("wrong choice")

#4
age = int(input("enter the age"))
if(age > 0  and age < 18):
  print("minor")
elif(age>=18  and age<=30):
  print("adult")
else:
  print("senior")

#5
for num in range(2, 51, 2):
  print(num)

#6
password=str(input("enter password "))
crct_password='gofhhf@2'
while (1):
  if password==crct_password:
    print("You have entered crct password")
    break

  else:
    print("enter crct pwd")
    break

#7
def calculate_average(n):
   return sum(n) / len(n)

n = [1, 2, 3, 4, 5]
avge = calculate_average(n)
print(avge)

#8
def count_vowels(text):
  vowels = 'aeiouAEIOU'
  count = 0
  for x in text:
    if x in vowels:
      count += 1
  return count
text = input("enter the string  ")
v_count = count_vowels(text)
print(v_count)

#9
import datetime

def current_datetime():
  now = datetime.datetime.now()
  f_dtme = now.strftime("%Y-%m-%d %H:%M:%S")
  print(f_dtme)


current_datetime()

#10
 def add_numbers(n1, n2):

  try:
    n1 = float(n1)
    n2 = float(n2)
    return n1 + n2
  except ValueError:

    print("Error: Invalid input. Please enter valid numbers only .")
    return None
n = add_numbers(1, 2)
print(n)

n = add_numbers("hello", 5)
print(n)

#11
def int_input(para):


  while True:
    try:
      b = input(para)
      return int(b)
    except ValueError:
      print("Invalid input. Please enter an integer value.")

n = int_input("Enter an integer: ")
print(f"You entered: {n}")

#12
def div(n, d):
  if d== 0:
    print("Error: Division by zero is not allowed.")
    return None
  else:
    return n / d

val = div(10, 2)
print(val)

val = div(5, 0)
print(val)

#13
def write_file(filename, content):
  try:
    with open(filename, 'w') as file:
      file.write(content)
    print(f"Success '{content}' to {filename}")
  except FileNotFoundError:
    print(f"Error: File '{filename}' does not exist.")
  except Exception as e:
    print(f"An error to  writing to the file: {e}")


filename = "dsailv.txt"
content = "hello python"
write_file(filename, content)

#14
filename = "dsailv.txt"
f = open(filename, "r")
print(content)

#15
a = " the add new line ."
content += a
print("Content of the file:")
print(content)

